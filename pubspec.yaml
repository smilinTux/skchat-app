name: skchat
description: "SKChat — Sovereign Encrypted P2P Communication"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  
  # State management
  flutter_riverpod: ^2.6.0
  freezed_annotation: ^2.4.0
  
  # Navigation
  go_router: ^14.0.0
  
  # HTTP client for SKComm daemon
  dio: ^5.7.0
  
  # Local storage
  hive_flutter: ^1.1.0
  
  # Notifications & system
  flutter_local_notifications: ^18.0.0
  path_provider: ^2.1.0
  local_auth: ^2.3.0
  
  # QR codes
  qr_flutter: ^4.1.0
  mobile_scanner: ^6.0.0
  
  # Utils
  share_plus: ^10.0.0
  url_launcher: ^6.3.0
  intl: ^0.19.0
  
  # UI enhancements
  shimmer: ^3.0.0
  lottie: ^3.1.0
  
  # Cryptography — pure-Dart RSA / AES primitives
  pointycastle: ^3.7.3

  # ASN1 encoding/decoding for DER key serialization
  asn1lib: ^1.5.0

  # Secure key storage (OS keychain / Keystore)
  flutter_secure_storage: ^9.0.0

  # Icons
  cupertino_icons: ^1.0.8

  # WebRTC voice/video calls
  flutter_webrtc: ^0.11.0
  web_socket_channel: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  # Code generation
  build_runner: ^2.4.0
  freezed: ^2.5.0
  json_serializable: ^6.8.0
  
  # Testing
  mocktail: ^1.0.0
  
  # Linting
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
